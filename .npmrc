# Suppress non-critical deprecation warnings
legacy-peer-deps=false
engine-strict=false

# Allow deprecated packages if necessary (Firebase dependency chain)
# fetch-blob@3.2.0 -> node-domexception is deprecated but safe
